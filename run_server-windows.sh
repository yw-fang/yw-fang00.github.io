bundle exec jekyll --watch
# in addition, you should open another terminal from the directory of _site, to run
# ruby -run -e httpd . -p 8080
# In this case, you can view the website in your local computer with Windows 10.
